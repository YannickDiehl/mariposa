% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wilcoxon_test.R
\name{wilcoxon_test}
\alias{wilcoxon_test}
\title{Compare Two Related Measurements Without Assuming Normality}
\usage{
wilcoxon_test(data, x, y, weights = NULL, conf.level = 0.95)
}
\arguments{
\item{data}{Your survey data (a data frame or tibble) in wide format,
with one row per subject and the two measurements in separate columns}

\item{x}{The first measurement variable (e.g., pre-test, trust in government)}

\item{y}{The second measurement variable (e.g., post-test, trust in media).
The difference is computed as \code{y - x}}

\item{weights}{Optional survey weights for population-representative results}

\item{conf.level}{Confidence level for intervals (Default: 0.95 = 95 percent)}
}
\value{
Test results showing whether the two measurements differ, including:
\itemize{
\item Z statistic (standardized test statistic, normal approximation)
\item P-value (is there a significant difference?)
\item Effect size r (how strong is the difference?)
\item Rank statistics (negative ranks, positive ranks, ties)
}
}
\description{
\code{wilcoxon_test()} compares two paired measurements from the same
subjects when your data isn't normally distributed. It's the non-parametric
alternative to the paired t-test.

Think of it as:
\itemize{
\item A way to test whether scores changed between two time points
\item Comparing ratings of two items from the same respondents
\item A robust paired comparison that works with any data shape
}

The test tells you:
\itemize{
\item Whether scores are significantly different between the two measurements
\item How many subjects increased, decreased, or stayed the same
\item The strength of the effect (effect size r)
}
}
\details{
\subsection{Understanding the Results}{

\strong{P-value}: If p < 0.05, the two measurements are significantly different
\itemize{
\item p < 0.001: Very strong evidence of a difference
\item p < 0.01: Strong evidence of a difference
\item p < 0.05: Moderate evidence of a difference
\item p > 0.05: No significant difference found
}

\strong{Effect Size r} (How strong is the difference?):
\itemize{
\item < 0.1: Negligible effect
\item 0.1 - 0.3: Small effect
\item 0.3 - 0.5: Medium effect
\item 0.5 or higher: Large effect
}

\strong{Rank Categories}:
\itemize{
\item Negative Ranks: subjects where y < x (decreased)
\item Positive Ranks: subjects where y > x (increased)
\item Ties: subjects where y = x (no change)
}
}

\subsection{When to Use This}{

Use Wilcoxon signed-rank test when:
\itemize{
\item Comparing two related measurements from the same subjects
\item Your data is not normally distributed
\item You have ordinal data (ratings, rankings)
\item Sample size is small
\item You want a robust alternative to the paired t-test
}
}

\subsection{Relationship to Other Tests}{
\itemize{
\item For normally distributed paired data: Use paired t-test instead
\item For independent groups: Use \code{\link{mann_whitney}()} instead
\item For 3+ related measurements: Use \code{friedman_test()} instead
}
}
}
\examples{
# Load required packages and data
library(dplyr)
data(survey_data)

# Compare trust in government vs trust in media
survey_data \%>\%
  wilcoxon_test(x = trust_government, y = trust_media)

# Weighted analysis
survey_data \%>\%
  wilcoxon_test(x = trust_government, y = trust_media,
                weights = sampling_weight)

# Grouped analysis (separate test per region)
survey_data \%>\%
  group_by(region) \%>\%
  wilcoxon_test(x = trust_government, y = trust_media)

}
\references{
Wilcoxon, F. (1945). Individual comparisons by ranking methods. Biometrics
Bulletin, 1(6), 80-83.

Fritz, C. O., Morris, P. E., & Richler, J. J. (2012). Effect size estimates:
current use, calculations, and interpretation. Journal of Experimental
Psychology: General, 141(1), 2.
}
\seealso{
\code{\link[stats]{wilcox.test}} for the base R Wilcoxon test.

\code{\link{mann_whitney}} for comparing two independent groups.

Other hypothesis_tests: 
\code{\link{binomial_test}()},
\code{\link{chi_square}()},
\code{\link{friedman_test}()},
\code{\link{kruskal_wallis}()},
\code{\link{mann_whitney}()},
\code{\link{oneway_anova}()},
\code{\link{t_test}()}
}
\concept{hypothesis_tests}
