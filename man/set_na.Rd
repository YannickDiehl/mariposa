% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_missing.R
\name{set_na}
\alias{set_na}
\title{Declare Values as Missing}
\usage{
set_na(data, ..., tag = TRUE, verbose = FALSE)
}
\arguments{
\item{data}{A data frame, tibble, or a single vector.}

\item{...}{Values to set as missing. Can be:
\itemize{
\item \strong{Unnamed numeric values}: Applied to all numeric columns
(e.g., \code{set_na(data, -9, -8)})
\item \strong{Named pairs}: Applied to specific variables
(e.g., \code{set_na(data, income = c(-9, -8), age = -1)})
}}

\item{tag}{If \code{TRUE} (default), uses tagged NAs to preserve distinct
missing types. The resulting tagged NAs integrate with
\code{\link[=na_frequencies]{na_frequencies()}}, \code{\link[=frequency]{frequency()}}, and \code{\link[=codebook]{codebook()}}.
If \code{FALSE}, replaces with regular \code{NA}.}

\item{verbose}{If \code{TRUE}, prints a summary of conversions.}
}
\value{
The modified data (invisibly for data frames, visibly for vectors).
}
\description{
Replaces specific numeric values with \code{NA} (or tagged NAs) across one or
more variables. This is essential for data cleaning workflows where missing
value codes (e.g., -9, -8, 99) are stored as regular values and need to be
declared as missing after import.
}
\details{
\subsection{Tagged vs. Regular NA}{

When \code{tag = TRUE} (default), each missing value code gets a unique tag
character, so you can distinguish between "No answer" (-9) and "Not
applicable" (-8) in downstream analysis. This is the same system used by
\code{\link[=read_spss]{read_spss()}} with \code{tag.na = TRUE}.

When \code{tag = FALSE}, all specified values become regular \code{NA} and the
distinction between different missing types is lost.
}

\subsection{Interaction with Existing Labels}{

If a value being set to missing has an existing value label, that label
is preserved as a tagged NA label (when \code{tag = TRUE}), making it visible
in \code{\link[=frequency]{frequency()}} and \code{\link[=codebook]{codebook()}} output.
}
}
\examples{
\dontrun{
# Set -9 and -8 as missing across all numeric variables
data <- set_na(survey_data, -9, -8)

# Set missing for specific variables only
data <- set_na(survey_data,
  income = c(-9, -8, -42),
  life_satisfaction = c(-9, -11)
)

# Use regular NA instead of tagged NA
data <- set_na(survey_data, -9, -8, tag = FALSE)

# Check the result
na_frequencies(data$income)
}

}
\seealso{
\code{\link[=na_frequencies]{na_frequencies()}} for inspecting missing types,
\code{\link[=strip_tags]{strip_tags()}} for converting tagged NAs to regular NA,
\code{\link[=untag_na]{untag_na()}} for recovering original codes

Other labels: 
\code{\link{copy_labels}()},
\code{\link{drop_labels}()},
\code{\link{find_var}()},
\code{\link{to_character}()},
\code{\link{to_label}()},
\code{\link{to_labelled}()},
\code{\link{to_numeric}()},
\code{\link{unlabel}()},
\code{\link{val_labels}()},
\code{\link{var_label}()}
}
\concept{labels}
