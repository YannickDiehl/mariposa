% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_sas.R
\name{read_sas}
\alias{read_sas}
\title{Read SAS Data with Tagged Missing Values}
\usage{
read_sas(
  path,
  catalog_file = NULL,
  encoding = NULL,
  catalog_encoding = NULL,
  tag_na = NULL,
  verbose = FALSE,
  tag.na = NULL
)
}
\arguments{
\item{path}{Path to a SAS \code{.sas7bdat} data file.}

\item{catalog_file}{Path to a SAS catalog file (\code{.sas7bcat}) for value
labels. If \code{NULL}, no catalog is used.}

\item{encoding}{Character encoding for the data file. If \code{NULL}, haven's
default encoding detection is used.}

\item{catalog_encoding}{Character encoding for the catalog file. If \code{NULL},
the same encoding as the data file is used.}

\item{tag_na}{Numeric vector of values to treat as missing (e.g.,
\code{c(-9, -8, -42)}). These values will be converted to tagged NAs across
all numeric variables. Use this when SAS files contain missing codes
stored as regular values. Default: \code{NULL} (only detect native SAS
special missing values).}

\item{verbose}{If \code{TRUE}, prints a message summarizing how many variables
contain tagged missing values.}

\item{tag.na}{Deprecated dot-case argument name. See the "Deprecated
arguments" section below.}
}
\value{
A tibble with the SAS data. Variables with missing value codes
have:
\itemize{
\item Tagged NAs for each missing type
\item An \code{"na_tag_map"} attribute mapping tag characters to original
codes
\item \code{is.na()} returns \code{TRUE} for these values (standard R behavior)
}
}
\description{
Reads a SAS \code{.sas7bdat} file and integrates with mariposa's tagged NA
system. Handles two scenarios:
\enumerate{
\item \strong{Native special missing values} (\code{.A} through \code{.Z}, \code{._}):
Automatically detected and annotated.
\item \strong{Numeric missing codes} (e.g., -9, -42): When \code{tag_na} is
provided, these regular values are converted to tagged NAs, giving the
same result as \code{\link[=read_spss]{read_spss()}} with \code{tag_na = TRUE}.
}
}
\details{
\subsection{Deprecated arguments}{

The dot-case argument name \code{tag.na} is deprecated; use the snake_case
equivalent \code{tag_na} instead. The old name still works but issues a
deprecation warning (once per session) and will be removed in a future
release.
}

\subsection{Native Special Missing Values}{

SAS supports 28 distinct missing value types: \code{.} (system missing), \code{.A}
through \code{.Z}, and \code{._} (underscore). The \code{haven} package preserves these
as tagged NAs automatically. \code{read_sas()} adds the \code{na_tag_map} attribute
so that mariposa's tagged NA functions work seamlessly.
}

\subsection{Numeric Missing Codes (tag_na)}{

Some SAS files store missing value codes as regular numeric values (e.g.,
-9 = "No answer"). The \code{tag_na} parameter converts these to tagged NAs,
enabling proper handling in \code{\link[=frequency]{frequency()}}, \code{\link[=codebook]{codebook()}}, and other
functions.

When \code{tag_na} is used, \code{\link[=untag_na]{untag_na()}} can recover the original numeric codes.
}
}
\examples{
\dontrun{
# Read SAS file with native special missing values
data <- read_sas("survey.sas7bdat")

# Read with catalog for value labels
data <- read_sas("survey.sas7bdat", catalog_file = "formats.sas7bcat")

# Read SAS file with numeric missing codes
data <- read_sas("survey.sas7bdat", tag_na = c(-9, -8, -42))

# Check what types of missing values exist
na_frequencies(data$income)

# Recover original codes or convert to regular NAs
untag_na(data$income)   # Recovers -9, -8, etc.
strip_tags(data$income) # Converts all to NA
}

}
\seealso{
\code{\link[=read_xpt]{read_xpt()}}, \code{\link[=na_frequencies]{na_frequencies()}}, \code{\link[=strip_tags]{strip_tags()}}, \code{\link[=untag_na]{untag_na()}},
\code{\link[=read_spss]{read_spss()}}, \code{\link[=read_stata]{read_stata()}}, \code{\link[haven:read_sas]{haven::read_sas()}}

Other data-import:
\code{\link[=na_frequencies]{na_frequencies()}},
\code{\link[=read_por]{read_por()}},
\code{\link[=read_spss]{read_spss()}},
\code{\link[=read_stata]{read_stata()}},
\code{\link[=read_xlsx]{read_xlsx()}},
\code{\link[=read_xpt]{read_xpt()}},
\code{\link[=strip_tags]{strip_tags()}},
\code{\link[=untag_na]{untag_na()}}
}
\concept{data-import}
