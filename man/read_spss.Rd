% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_spss.R
\name{read_spss}
\alias{read_spss}
\title{Read SPSS Data with Tagged Missing Values}
\usage{
read_spss(path, tag.na = TRUE, encoding = NULL, verbose = FALSE)
}
\arguments{
\item{path}{Path to an SPSS \code{.sav} file.}

\item{tag.na}{If \code{TRUE} (the default), user-defined missing values are
converted to tagged NAs using \code{\link[haven:tagged_na]{haven::tagged_na()}}. If \code{FALSE}, the file
is read with standard \code{haven::read_sav()} behavior (all missing values
become regular \code{NA}).}

\item{encoding}{Character encoding for the file. If \code{NULL}, haven's default
encoding detection is used.}

\item{verbose}{If \code{TRUE}, prints a message summarizing how many values were
converted.}
}
\value{
A tibble with the SPSS data. When \code{tag.na = TRUE}:
\itemize{
\item User-defined missing values are stored as tagged NAs
\item \code{is.na()} returns \code{TRUE} for these values (standard R behavior)
\item The original SPSS missing codes can be recovered via
\code{\link[=na_frequencies]{na_frequencies()}}, \code{\link[=untag_na]{untag_na()}}, or \code{\link[haven:tagged_na]{haven::na_tag()}}
\item Each tagged variable has an \code{"na_tag_map"} attribute mapping
tag characters to original SPSS codes
}
}
\description{
Reads an SPSS \code{.sav} file and preserves user-defined missing values as
tagged NAs instead of converting them to regular \code{NA}. This allows you to
distinguish between different types of missing data (e.g., "no answer",
"not applicable", "refused") while still treating them as \code{NA} in
standard R operations.
}
\details{
SPSS allows defining specific values as "user-defined missing values"
(e.g., -9 = "no answer", -8 = "don't know"). When reading \code{.sav} files
with \code{haven::read_sav()}, these are silently converted to \code{NA}, losing the
information about \emph{why} a value is missing.

\code{read_spss()} preserves this information using haven's tagged NA system:
each missing value type gets a unique tag character (a-z, A-Z, 0-9) that
can be inspected with \code{\link[haven:tagged_na]{haven::na_tag()}}. The values still behave as \code{NA}
in all standard R operations (\code{mean()}, \code{sum()}, \code{is.na()}, etc.).

Use the companion functions to work with the tagged NAs:
\itemize{
\item \code{\link[=na_frequencies]{na_frequencies()}} - Frequency table of missing types
\item \code{\link[=untag_na]{untag_na()}} - Convert tagged NAs back to original SPSS codes
\item \code{\link[=strip_tags]{strip_tags()}} - Convert tagged NAs to regular NAs (drop tags)
}
}
\examples{
\dontrun{
# Read SPSS file with tagged missing values
data <- read_spss("survey.sav")

# Check what types of missing values exist
na_frequencies(data$satisfaction)

# Standard R operations work normally (NAs are excluded)
mean(data$satisfaction, na.rm = TRUE)

# frequency() shows each missing type separately
data \%>\% frequency(satisfaction)

# Recover original SPSS codes
original_codes <- untag_na(data$satisfaction)

# Convert to regular NAs (standard behavior)
data_clean <- strip_tags(data$satisfaction)
}

}
\seealso{
\code{\link[=na_frequencies]{na_frequencies()}}, \code{\link[=untag_na]{untag_na()}}, \code{\link[=strip_tags]{strip_tags()}}, \code{\link[haven:read_spss]{haven::read_sav()}},
\code{\link[=frequency]{frequency()}}

Other spss: 
\code{\link{na_frequencies}()},
\code{\link{strip_tags}()},
\code{\link{untag_na}()}
}
\concept{spss}
