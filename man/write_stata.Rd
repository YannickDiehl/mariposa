% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_stata.R
\name{write_stata}
\alias{write_stata}
\title{Export Data to Stata Format}
\usage{
write_stata(data, path, version = 14)
}
\arguments{
\item{data}{A data frame to export. Columns of class \code{haven_labelled} will
have their labels written to the \code{.dta} file.}

\item{path}{Path to the output file. Must end in \code{.dta}.}

\item{version}{Stata file version to use. Supported values: 8-15.
Default is 14 (Stata 14/15, supports Unicode). Use 13 for compatibility
with older Stata versions.}
}
\value{
Invisibly returns the file path.
}
\description{
Writes a data frame to a Stata \code{.dta} file, preserving variable labels,
value labels, and missing value types. Tagged NAs from any source format
(SPSS, Stata, SAS) are written as Stata extended missing values
(\code{.a} through \code{.z}).
}
\details{
\subsection{Tagged NA Handling}{

Stata natively supports 27 extended missing values: \code{.a} through \code{.z} and
\code{.} (system missing). When exporting data with tagged NAs:
\itemize{
\item \strong{From Stata} (\code{read_stata()}): Native extended missing values
are preserved in a full roundtrip.
\item \strong{From SPSS} (\code{read_spss()}): Tagged NAs are written as Stata
extended missing values (\code{.a}, \code{.b}, etc.). The original SPSS numeric
codes (e.g., -9, -8) are not preserved in the Stata file, but the
distinct missing value types and their labels are.
\item \strong{From SAS} (\code{read_sas()}, \code{read_xpt()}): SAS special missing
values are mapped to their Stata equivalents.
}
}

\subsection{Limitations}{

Stata supports at most 27 distinct missing types per variable. SPSS data
with more than 27 tagged NA types in a single variable may lose some
distinctions (though this is extremely rare in practice).
}
}
\examples{
\dontrun{
# Roundtrip: read Stata, process, write back
data <- read_stata("survey.dta")
data_clean <- data[data$age >= 18, ]
write_stata(data_clean, "survey_adults.dta")

# Cross-format: SPSS to Stata
data <- read_spss("survey.sav")
write_stata(data, "survey.dta")

# Stata 13 compatibility
write_stata(data, "survey_v13.dta", version = 13)
}

}
\seealso{
\code{\link[=read_stata]{read_stata()}} for importing Stata files,
\code{\link[=write_spss]{write_spss()}} for SPSS export,
\code{\link[=write_xlsx]{write_xlsx()}} for Excel export,
\code{\link[=write_xpt]{write_xpt()}} for SAS transport export

Other data-export: 
\code{\link{write_spss}()},
\code{\link{write_xlsx}()},
\code{\link{write_xpt}()}
}
\concept{data-export}
