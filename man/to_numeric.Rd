% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_conversion.R
\name{to_numeric}
\alias{to_numeric}
\title{Convert Factors or Labelled Variables to Numeric}
\usage{
to_numeric(
  data,
  ...,
  use_labels = TRUE,
  start_at = NULL,
  keep_labels = FALSE,
  use.labels = NULL,
  start.at = NULL,
  keep.labels = NULL
)
}
\arguments{
\item{data}{A data frame, tibble, or a single vector.}

\item{...}{Optional: unquoted variable names (tidyselect supported). If
empty, converts all factor columns.}

\item{use_labels}{If \code{TRUE} (default), attempts to use the numeric value
of factor levels (e.g., level \code{"3"} becomes \code{3}). If \code{FALSE}, uses
sequential integers (1, 2, 3, ...).}

\item{start_at}{If not \code{NULL}, the lowest numeric value in the output
starts at this number. Default: \code{NULL} (use original values).}

\item{keep_labels}{If \code{TRUE}, the former factor levels are stored as
value labels on the result. Default: \code{FALSE}.}

\item{use.labels, start.at, keep.labels}{Defunct dot-case argument names,
removed in mariposa 0.6.9. Calling the function with any of them is an
error; use the snake_case equivalents instead. (The formals are
retained only so that the old names error clearly instead of being
swallowed by \code{...}.)}
}
\value{
The input with variables converted to numeric. For single vector
input, returns a numeric vector.
}
\description{
Converts factor levels or labelled values to numeric. For factors, uses
the underlying integer codes (or the numeric value of levels if they are
numeric strings). For labelled variables, extracts the underlying numeric
values.
}
\details{
This function handles three input types:
\enumerate{
\item \strong{Numeric factors} (levels like \code{"1"}, \code{"2"}, \code{"3"}): Extracts
the numeric values from the level names.
\item \strong{Text factors} (levels like \code{"Male"}, \code{"Female"}): Converts
to sequential integers by default; use \code{use_labels = FALSE} to force
this behavior even for numeric-looking levels.
\item \strong{haven_labelled}: Extracts the underlying numeric vector,
stripping the labelled class.
}
}
\examples{
# Numeric factor levels → numeric
x <- factor(c("1", "3", "5", "3"))
to_numeric(x)
# [1] 1 3 5 3

# Sequential integers
to_numeric(x, use_labels = FALSE)
# [1] 1 2 3 2

# Haven labelled → plain numeric
to_numeric(survey_data$life_satisfaction)

}
\seealso{
\code{\link[=to_label]{to_label()}} for the reverse (numeric → factor),
\code{\link[=to_labelled]{to_labelled()}} for creating labelled vectors

Other labels:
\code{\link[=copy_labels]{copy_labels()}},
\code{\link[=drop_labels]{drop_labels()}},
\code{\link[=find_var]{find_var()}},
\code{\link[=set_na]{set_na()}},
\code{\link[=to_character]{to_character()}},
\code{\link[=to_label]{to_label()}},
\code{\link[=to_labelled]{to_labelled()}},
\code{\link[=unlabel]{unlabel()}},
\code{\link[=val_labels]{val_labels()}},
\code{\link[=var_label]{var_label()}}
}
\concept{labels}
