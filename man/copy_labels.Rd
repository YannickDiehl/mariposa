% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_management.R
\name{copy_labels}
\alias{copy_labels}
\title{Copy Labels from One Data Frame to Another}
\usage{
copy_labels(data, source)
}
\arguments{
\item{data}{The target data frame (e.g., after filtering or subsetting).}

\item{source}{The source data frame with the original labels.}
}
\value{
The target data frame with labels copied from the source. Only
columns present in both data frames are affected. Columns only in
the target are left unchanged.
}
\description{
Copies variable labels, value labels, and tagged NA metadata from a source
data frame to a target data frame. This is essential after dplyr operations
like \code{\link[dplyr:filter]{dplyr::filter()}}, \code{\link[dplyr:select]{dplyr::select()}}, or \code{\link[dplyr:mutate]{dplyr::mutate()}} which can
strip label attributes.
}
\details{
The following attributes are copied for each shared column:
\itemize{
\item \code{"label"} — variable label
\item \code{"labels"} — value labels
\item \code{"na_tag_map"} — tagged NA mapping
\item \code{"na_tag_format"} — tagged NA format (spss/stata/sas)
\item \code{"class"} — vector class (e.g., \code{haven_labelled})
}
}
\examples{
# Labels are lost after dplyr operations
data_subset <- dplyr::filter(survey_data, age >= 18)

# Restore them
data_subset <- copy_labels(data_subset, survey_data)

}
\seealso{
\code{\link[=var_label]{var_label()}}, \code{\link[=val_labels]{val_labels()}}

Other labels: 
\code{\link{drop_labels}()},
\code{\link{find_var}()},
\code{\link{set_na}()},
\code{\link{to_character}()},
\code{\link{to_label}()},
\code{\link{to_labelled}()},
\code{\link{to_numeric}()},
\code{\link{unlabel}()},
\code{\link{val_labels}()},
\code{\link{var_label}()}
}
\concept{labels}
