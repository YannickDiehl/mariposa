% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_xlsx.R
\name{read_xlsx}
\alias{read_xlsx}
\title{Read Excel Data with Label Reconstruction}
\usage{
read_xlsx(path, sheet = NULL, labels = TRUE, verbose = FALSE, ...)
}
\arguments{
\item{path}{Path to the \code{.xlsx} file.}

\item{sheet}{Sheet to read. \code{NULL} (default) auto-detects: reads the
"Data" sheet if present, otherwise the first non-metadata sheet.
Can be a sheet name (character) or index (integer).}

\item{labels}{If \code{TRUE} (default), detects a "Labels" sheet and
reconstructs \code{haven_labelled} columns, factor levels, variable labels,
and tagged NA metadata. Set to \code{FALSE} to read raw data without
label reconstruction.}

\item{verbose}{If \code{TRUE}, prints a summary of reconstructed labels
and tagged NA mappings.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A tibble. When a mariposa-formatted Labels sheet is detected:
\itemize{
\item Numeric columns with value labels are returned as
\code{haven_labelled} vectors
\item Factor columns are reconstructed with their original levels
\item Variable labels are attached via \code{attr(x, "label")}
\item Tagged NAs are restored with \code{na_tag_map} and \code{na_tag_format}
attributes
}
}
\description{
Reads an Excel (\code{.xlsx}) file and returns a tibble. When the file was
created by \code{\link[=write_xlsx]{write_xlsx()}}, the accompanying "Labels" sheet is automatically
detected and used to reconstruct variable labels, value labels, and tagged
NA metadata -- giving you the same labelled structure as the original data.

For plain Excel files (without a "Labels" sheet), this function works like
a convenient wrapper around the \code{openxlsx2} reader, returning a clean tibble.
}
\details{
\subsection{Roundtrip Workflow}{

\code{read_xlsx()} is designed to work seamlessly with \code{\link[=write_xlsx]{write_xlsx()}}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{write_xlsx(data, "survey.xlsx")   # Exports data + Labels sheet
data2 <- read_xlsx("survey.xlsx") # Reconstructs labels from Labels sheet
}\if{html}{\out{</div>}}

The reconstructed data will have the same variable labels, value labels,
and tagged NA codes as the original. System NAs (empty cells in Excel)
remain as regular \code{NA}.
}

\subsection{File Format Detection}{

\code{read_xlsx()} auto-detects the type of mariposa export:
\itemize{
\item \strong{data.frame export} ("Data" + "Labels" sheets): Reads the
"Data" sheet and applies labels.
\item \strong{list export} (multiple data sheets + "Labels" with "Sheet"
column): Reads the specified or first data sheet and applies
matching labels.
\item \strong{codebook export} ("Overview" + "Codebook" sheets): Warns
that this is a documentation file and reads it as a plain table.
\item \strong{plain Excel}: No Labels sheet detected; returns raw data.
}
}

\subsection{When to Use This}{

Use \code{read_xlsx()} when you:
\itemize{
\item Want to read back data exported with \code{write_xlsx()}
\item Need a quick way to import any \code{.xlsx} file as a tibble
\item Want to share labelled survey data via Excel and read it back
with all metadata intact
}
}
}
\examples{
\dontrun{
# Read back a mariposa-exported file with full label reconstruction
data <- read_xlsx("survey_export.xlsx")
attr(data$gender, "labels")   # Value labels restored
attr(data$gender, "label")    # Variable label restored

# Read a plain Excel file
data <- read_xlsx("plain_data.xlsx")

# Read a specific sheet
data <- read_xlsx("multi_sheet.xlsx", sheet = "Sheet2")

# Skip label reconstruction
raw <- read_xlsx("survey_export.xlsx", labels = FALSE)
}

}
\seealso{
\code{\link[=write_xlsx]{write_xlsx()}} for exporting data with labels,
\code{\link[=read_spss]{read_spss()}}, \code{\link[=read_stata]{read_stata()}}, \code{\link[=read_sas]{read_sas()}} for importing from
statistical software

Other data-import: 
\code{\link{na_frequencies}()},
\code{\link{read_por}()},
\code{\link{read_sas}()},
\code{\link{read_spss}()},
\code{\link{read_stata}()},
\code{\link{read_xpt}()},
\code{\link{strip_tags}()},
\code{\link{untag_na}()}
}
\concept{data-import}
