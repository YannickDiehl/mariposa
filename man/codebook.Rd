% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codebook.R
\name{codebook}
\alias{codebook}
\title{Create a Codebook for Your Data}
\usage{
codebook(
  data,
  ...,
  weights = NULL,
  show_id = TRUE,
  show_type = TRUE,
  show_labels = TRUE,
  show_values = TRUE,
  show_freq = TRUE,
  show_na = TRUE,
  show_unused = FALSE,
  max_values = 10,
  max_len = 50,
  sort_by_name = FALSE,
  file = NULL
)
}
\arguments{
\item{data}{Your survey data (a data frame or tibble)}

\item{...}{Optional: specific variables to include. If empty, all variables
are shown. Supports tidyselect helpers like \code{starts_with("trust")}.}

\item{weights}{Optional survey weights for weighted frequency calculations}

\item{show_id}{Show variable position number? (Default: TRUE)}

\item{show_type}{Show data type? (Default: TRUE)}

\item{show_labels}{Show variable labels? (Default: TRUE)}

\item{show_values}{Show empirical values and value labels? (Default: TRUE)}

\item{show_freq}{Show frequency counts? (Default: TRUE)}

\item{show_na}{Show tagged missing value types in the codebook? (Default:
TRUE). When data was imported with \code{\link[=read_spss]{read_spss()}}, \code{\link[=read_stata]{read_stata()}},
\code{\link[=read_sas]{read_sas()}}, or \code{\link[=read_xpt]{read_xpt()}} using tagged NAs, they are displayed
with their original missing value codes, labels, and frequencies below
the valid values, separated by a thin gray line.}

\item{show_unused}{Show all defined value labels, even those with zero
observations? (Default: FALSE). Useful for seeing the full codebook
including response options that no respondent selected.}

\item{max_values}{Maximum number of values to display per variable
before truncating or showing a range (Default: 10)}

\item{max_len}{Maximum character width for labels before truncation
(Default: 50)}

\item{sort_by_name}{Sort variables alphabetically instead of by position?
(Default: FALSE)}

\item{file}{Path to save the HTML codebook. If NULL (default), a temporary
file is used and the codebook opens in the Viewer.}
}
\value{
Invisibly returns a list of class \code{"codebook"} containing:
\describe{
\item{codebook}{Tibble with one row per variable and all metadata}
\item{data_info}{List with dataset-level information (name, nrow, ncol, etc.)}
\item{html}{The generated HTML as an htmltools object}
\item{weights}{Name of the weight variable, or NULL}
\item{options}{List of all display options}
\item{frequencies}{Named list of frequency tables per variable}
}
}
\description{
\code{codebook()} creates an interactive HTML data dictionary that opens in the
RStudio Viewer. It shows you everything about your dataset at a glance:
variable names, types, labels, the empirical values found in the data,
value labels, and frequency counts.

Think of it as a professional "cheat sheet" for your dataset -- especially
useful when working with labelled survey data imported from SPSS, Stata,
or SAS.
}
\details{
\subsection{What the Codebook Shows}{

For each variable, the codebook displays (depending on options):
\itemize{
\item \strong{ID}: Column position in the dataset
\item \strong{Name}: Variable name (in monospace font)
\item \strong{Type}: Data type (numeric, factor, ordered factor, haven_labelled, etc.)
\item \strong{Label}: Variable label (from SPSS/Stata/SAS imports or manual assignment)
\item \strong{Values}: The empirical values found in the data
\item \strong{Value Labels}: Labels assigned to those values (if any)
\item \strong{Freq.}: Frequency count for each value
}
}

\subsection{When to Use This}{

Use \code{codebook()} when you:
\itemize{
\item First receive a new dataset and want to understand its structure
\item Work with labelled data (SPSS, Stata, SAS) and need to see all value labels
\item Want to document your dataset for colleagues or publications
\item Need to quickly see value distributions across variables
}
}
}
\examples{
data(survey_data)

# Compact console overview (no Viewer)
cb <- codebook(survey_data)
print(cb)

# Detailed console output
summary(cb)

\donttest{
# Full codebook (opens in RStudio Viewer when interactive)
codebook(survey_data)

# Only trust-related variables
codebook(survey_data, starts_with("trust"))

# Save to file for sharing
codebook(survey_data, file = tempfile(fileext = ".html"))
}

}
\seealso{
\code{\link[=describe]{describe()}} for detailed numeric summaries, \code{\link[=frequency]{frequency()}} for
detailed frequency tables, \code{\link[=read_spss]{read_spss()}}, \code{\link[=read_por]{read_por()}}, \code{\link[=read_stata]{read_stata()}},
\code{\link[=read_sas]{read_sas()}}, \code{\link[=read_xpt]{read_xpt()}} for importing data with tagged NAs
}
