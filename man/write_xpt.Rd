% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_sas.R
\name{write_xpt}
\alias{write_xpt}
\title{Export Data to SAS Transport Format}
\usage{
write_xpt(data, path, version = 5, name = NULL)
}
\arguments{
\item{data}{A data frame to export.}

\item{path}{Path to the output file. Must end in \code{.xpt}.}

\item{version}{SAS transport file version. Either \code{5} (default, SAS
Transport v5, most compatible) or \code{8} (SAS Transport v8, supports
longer variable names).}

\item{name}{Member name for the dataset within the transport file. If
\code{NULL}, derived from the file name. Maximum 8 characters for version 5.}
}
\value{
Invisibly returns the file path.
}
\description{
Writes a data frame to a SAS transport file (\code{.xpt}), preserving variable
labels and missing value types. Tagged NAs from any source format (SPSS,
Stata, SAS) are written as SAS special missing values (\code{.A} through \code{.Z},
\code{._}).
}
\details{
\subsection{Tagged NA Handling}{

SAS supports 28 special missing values: \code{.} (system missing), \code{.A}
through \code{.Z}, and \code{._}. When exporting data with tagged NAs:
\itemize{
\item \strong{From SAS} (\code{read_sas()}, \code{read_xpt()}): Native special missing
values are preserved in a full roundtrip.
\item \strong{From SPSS} (\code{read_spss()}): Tagged NAs are written as SAS
special missing values. The original SPSS numeric codes (e.g., -9, -8)
are not preserved, but the distinct missing value types are.
\item \strong{From Stata} (\code{read_stata()}): Stata extended missing values
are mapped to their SAS equivalents.
}
}

\subsection{Limitations}{

SAS transport files do \strong{not} store value labels. If your data has value
labels, consider using \code{\link[=write_spss]{write_spss()}} or \code{\link[=write_xlsx]{write_xlsx()}} instead, which
preserve full label metadata. Variable labels are preserved.
}
}
\examples{
\dontrun{
# Roundtrip: read SAS transport, process, write back
data <- read_xpt("survey.xpt")
data_clean <- data[data$age >= 18, ]
write_xpt(data_clean, "survey_adults.xpt")

# Cross-format: SPSS to SAS transport
data <- read_spss("survey.sav")
write_xpt(data, "survey.xpt")
}

}
\seealso{
\code{\link[=read_xpt]{read_xpt()}} and \code{\link[=read_sas]{read_sas()}} for importing SAS files,
\code{\link[=write_spss]{write_spss()}} for SPSS export,
\code{\link[=write_stata]{write_stata()}} for Stata export,
\code{\link[=write_xlsx]{write_xlsx()}} for Excel export

Other data-export: 
\code{\link{write_spss}()},
\code{\link{write_stata}()},
\code{\link{write_xlsx}()}
}
\concept{data-export}
