% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_conversion.R
\name{to_labelled}
\alias{to_labelled}
\title{Convert Variables to Labelled Format}
\usage{
to_labelled(data, ..., labels = NULL, label = NULL)
}
\arguments{
\item{data}{A data frame, tibble, or a single vector.}

\item{...}{Optional: unquoted variable names (tidyselect supported). If
empty on a data frame, converts all factor columns.}

\item{labels}{Optional: a named numeric vector of value labels to assign.
If \code{NULL} and the input is a factor, factor levels are used as labels.}

\item{label}{Optional: a variable label string.}
}
\value{
The input with variables converted to \code{haven_labelled}. For
single vector input, returns a \code{haven_labelled} vector.
}
\description{
Converts factors, character vectors, or plain numeric vectors to
\code{haven_labelled} class, optionally assigning value labels and a variable
label. This is the reverse of \code{\link[=to_label]{to_label()}}.
}
\details{
\subsection{Factor Conversion}{

When converting a factor, the integer codes (1, 2, 3, ...) become the
numeric values and the factor levels become the value labels.
}

\subsection{Character Conversion}{

Character vectors are converted to numeric (sequential integers) with
the unique character values as labels.
}
}
\examples{
# Factor → haven_labelled
x <- factor(c("Male", "Female", "Male"))
to_labelled(x)

# Numeric with custom labels
to_labelled(c(1, 2, 3),
  labels = c("Low" = 1, "Medium" = 2, "High" = 3),
  label = "Satisfaction level"
)

# All factors in a data frame
data <- to_labelled(survey_data)

}
\seealso{
\code{\link[=to_label]{to_label()}} for the reverse operation, \code{\link[=val_labels]{val_labels()}} for
setting labels on existing labelled vectors

Other labels: 
\code{\link{copy_labels}()},
\code{\link{drop_labels}()},
\code{\link{find_var}()},
\code{\link{set_na}()},
\code{\link{to_character}()},
\code{\link{to_label}()},
\code{\link{to_numeric}()},
\code{\link{unlabel}()},
\code{\link{val_labels}()},
\code{\link{var_label}()}
}
\concept{labels}
