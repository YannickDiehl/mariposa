% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_management.R
\name{val_labels}
\alias{val_labels}
\title{Get or Set Value Labels}
\usage{
val_labels(data, ..., .add = FALSE, drop.na = TRUE)
}
\arguments{
\item{data}{A data frame, tibble, or a single vector.}

\item{...}{In GET mode: unquoted variable names (tidyselect supported).
In SET mode: named pairs where the name is a variable and the value is
a named vector of labels (e.g., \code{c("Male" = 1, "Female" = 2)}).
Use \code{NULL} to remove all value labels from a variable.}

\item{.add}{If \code{TRUE}, adds labels to any existing ones instead of
replacing them. Default: \code{FALSE}.}

\item{drop.na}{If \code{TRUE} (default), tagged NA labels are excluded from
GET results.}
}
\value{
\itemize{
\item \strong{GET mode (vector input)}: A named numeric vector of value
labels, or \code{NULL}.
\item \strong{GET mode (data frame, single variable)}: A named numeric
vector of value labels.
\item \strong{GET mode (data frame, multiple variables)}: A named list of
label vectors.
\item \strong{SET mode}: The modified data frame (invisibly).
}
}
\description{
Retrieves or assigns value labels (the \code{"labels"} attribute) on labelled
vectors or data frame columns. Value labels map numeric codes to
descriptive text (e.g., \code{1 = "Male"}, \code{2 = "Female"}).

The function operates in two modes:
\itemize{
\item \strong{GET mode}: Called with bare variable names, returns existing
value labels.
\item \strong{SET mode}: Called with \code{name = c(...)} pairs, assigns value
labels to variables.
}
}
\details{
Value labels are stored as the \code{"labels"} attribute in haven's format:
a named numeric vector where names are the label text and values are
the numeric codes. This is the standard used by \code{\link[=read_spss]{read_spss()}},
\code{\link[=read_stata]{read_stata()}}, and \code{\link[=read_sas]{read_sas()}}.
\subsection{Adding vs. Replacing Labels}{

By default, SET mode replaces all existing value labels. Use \code{.add = TRUE}
to keep existing labels and only add new ones. If a value already has a
label, the new label overwrites it.
}
}
\examples{
# GET: retrieve value labels from a vector
val_labels(survey_data$gender)

# GET: from specific variables (returns list)
val_labels(survey_data, gender, region)

# SET: assign value labels
data <- val_labels(survey_data,
  gender = c("Male" = 1, "Female" = 2, "Non-binary" = 3)
)

# ADD: add labels without removing existing ones
data <- val_labels(data,
  gender = c("Prefer not to say" = 4),
  .add = TRUE
)

# REMOVE: set to NULL
data <- val_labels(data, gender = NULL)

}
\seealso{
\code{\link[=var_label]{var_label()}} for variable labels, \code{\link[=to_label]{to_label()}} for converting
labelled vectors to factors, \code{\link[=drop_labels]{drop_labels()}} for removing unused labels

Other labels: 
\code{\link{copy_labels}()},
\code{\link{drop_labels}()},
\code{\link{find_var}()},
\code{\link{set_na}()},
\code{\link{to_character}()},
\code{\link{to_label}()},
\code{\link{to_labelled}()},
\code{\link{to_numeric}()},
\code{\link{unlabel}()},
\code{\link{var_label}()}
}
\concept{labels}
