% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/row_operations.R
\name{row_count}
\alias{row_count}
\title{Count Occurrences of a Value Across Columns}
\usage{
row_count(data, ..., count, na.rm = TRUE)
}
\arguments{
\item{data}{Your survey data (a data frame or tibble).}

\item{...}{The variables to check. Supports tidyselect.}

\item{count}{The value to count.}

\item{na.rm}{If \code{TRUE} (default), \code{NA} values are ignored.
If \code{FALSE}, any row containing \code{NA} returns \code{NA}.}
}
\value{
An integer vector with one value per row — the count of how
often \code{count} appears.
}
\description{
Counts how often a specific value appears in each row across the selected
variables. Useful for data quality checks (e.g., "How many items did a
respondent answer with -9?") or for creating count-based indices.
}
\examples{
library(dplyr)
data(survey_data)

# How many items did each respondent answer with the highest value (5)?
survey_data <- survey_data \%>\%
  mutate(n_top = row_count(., trust_government, trust_media,
                           trust_science, count = 5))

}
\seealso{
\code{\link[=row_sums]{row_sums()}} for row-wise sums, \code{\link[=row_means]{row_means()}} for row-wise means

Other scale: 
\code{\link{efa}()},
\code{\link{pomps}()},
\code{\link{reliability}()},
\code{\link{row_means}()},
\code{\link{row_sums}()}
}
\concept{scale}
