% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chi_square.R
\name{phi}
\alias{phi}
\alias{cramers_v}
\alias{goodman_gamma}
\title{Effect Sizes for Contingency Tables}
\usage{
phi(data, ..., weights = NULL)

cramers_v(data, ..., weights = NULL)

goodman_gamma(data, ..., weights = NULL)
}
\arguments{
\item{data}{Your survey data (a data frame or tibble)}

\item{...}{Exactly two categorical variables, as in \code{chi_square()}}

\item{weights}{Optional survey weights}
}
\value{
A numeric vector with the effect size (one element per group for
grouped data).
}
\description{
Convenience helpers that run \code{\link{chi_square}} and return just the
requested effect size as a numeric value (named by group for grouped
data): \code{phi()} for 2x2 tables, \code{cramers_v()} for larger tables,
and \code{goodman_gamma()} for ordinal variables.

For the full test output (chi-square statistic, p-value, all effect
sizes), call \code{\link{chi_square}} directly.
}
\examples{
data(survey_data)
phi(survey_data, gender, region)
cramers_v(survey_data, education, region)
goodman_gamma(survey_data, education, life_satisfaction)

}
\seealso{
\code{\link{chi_square}}
}
\concept{effect_sizes}
