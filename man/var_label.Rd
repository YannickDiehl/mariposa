% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_management.R
\name{var_label}
\alias{var_label}
\title{Get or Set Variable Labels}
\usage{
var_label(data, ...)
}
\arguments{
\item{data}{A data frame, tibble, or a single vector.}

\item{...}{In GET mode: unquoted variable names (tidyselect supported).
If empty, returns labels for all variables.
In SET mode: named pairs where the name is a variable and the value is
the label string. Use \code{NULL} to remove a label.}
}
\value{
\itemize{
\item \strong{GET mode (vector input)}: A single character string (the label),
or \code{NULL} if no label exists.
\item \strong{GET mode (data frame input)}: A named character vector of labels.
Variables without labels return \code{NA}.
\item \strong{SET mode}: The modified data frame (invisibly), with labels
assigned.
}
}
\description{
Retrieves or assigns variable labels (the \code{"label"} attribute) on vectors
or data frame columns. Variable labels describe what a variable measures
(e.g., "Age of respondent") and are commonly used in survey data imported
from SPSS, Stata, or SAS.

The function operates in two modes:
\itemize{
\item \strong{GET mode}: Called with bare variable names or no \code{...} arguments,
returns existing labels.
\item \strong{SET mode}: Called with \code{name = "label"} pairs, assigns labels to
variables and returns the modified data.
}
}
\details{
Variable labels are stored as the \code{"label"} attribute on each column,
which is the standard used by the \code{haven} package for SPSS/Stata/SAS
imports. These labels are preserved by mariposa's \code{\link[=codebook]{codebook()}},
\code{\link[=frequency]{frequency()}}, and \code{\link[=describe]{describe()}} functions.
\subsection{When to Use This}{

Use \code{var_label()} when you:
\itemize{
\item Want to inspect what variables measure in imported survey data
\item Need to add labels to manually created data before using
\code{\link[=codebook]{codebook()}} or \code{\link[=write_spss]{write_spss()}}
\item Want to update or correct labels after import
}
}
}
\examples{
# GET: retrieve all variable labels
var_label(survey_data)

# GET: specific variables
var_label(survey_data, age, gender)

# GET: from a single vector
var_label(survey_data$age)

# SET: assign labels
data <- var_label(survey_data,
  age = "Age of respondent",
  gender = "Gender identity"
)

# REMOVE: set to NULL
data <- var_label(data, age = NULL)

}
\seealso{
\code{\link[=val_labels]{val_labels()}} for value labels, \code{\link[=codebook]{codebook()}} for viewing all
metadata, \code{\link[=copy_labels]{copy_labels()}} for preserving labels after dplyr operations

Other labels: 
\code{\link{copy_labels}()},
\code{\link{drop_labels}()},
\code{\link{find_var}()},
\code{\link{set_na}()},
\code{\link{to_character}()},
\code{\link{to_label}()},
\code{\link{to_labelled}()},
\code{\link{to_numeric}()},
\code{\link{unlabel}()},
\code{\link{val_labels}()}
}
\concept{labels}
