% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_spss.R
\name{write_spss}
\alias{write_spss}
\title{Export Data to SPSS Format}
\usage{
write_spss(data, path, compress = c("byte", "none", "zsav"))
}
\arguments{
\item{data}{A data frame to export. Columns of class \code{haven_labelled} will
have their labels and missing value metadata written to the \code{.sav} file.}

\item{path}{Path to the output file. Must end in \code{.sav} or \code{.zsav}.}

\item{compress}{Compression type. One of \code{"byte"} (default, byte-level
compression), \code{"none"} (no compression), or \code{"zsav"} (zlib compression,
requires SPSS v21+).}
}
\value{
Invisibly returns the file path.
}
\description{
Writes a data frame to an SPSS \code{.sav} file, preserving variable labels,
value labels, and user-defined missing values. When exporting data that
was imported with \code{\link[=read_spss]{read_spss()}} (with \code{tag.na = TRUE}), the tagged NAs are
automatically converted back to SPSS user-defined missing values, enabling
full roundtrip fidelity.
}
\details{
\subsection{Tagged NA Roundtripping}{

Data imported via \code{\link[=read_spss]{read_spss()}} stores SPSS user-defined missing values as
tagged NAs with an \code{na_tag_map} attribute mapping tag characters to original
codes (e.g., -9, -8). \code{write_spss()} reverses this process: tagged NAs are
converted back to their original numeric codes, and the SPSS user-defined
missing value specification is reconstructed so that the exported \code{.sav}
file has the same missing value definitions as the original.
}

\subsection{Cross-Format Export}{

When exporting data originally imported from Stata or SAS (with native
extended missing values like \code{.a}-\code{.z} or \code{.A}-\code{.Z}), these cannot be
represented as SPSS user-defined missing values. In this case, they are
written as system missing (regular \code{NA}) with a warning.
}

\subsection{When to Use This}{

Use \code{write_spss()} when you:
\itemize{
\item Need to export processed survey data back to SPSS format
\item Want to preserve user-defined missing value definitions
\item Need roundtrip fidelity: \code{read_spss()} -> processing -> \code{write_spss()}
}
}
}
\examples{
\dontrun{
# Roundtrip: read SPSS, process, write back
data <- read_spss("survey.sav")
data_clean <- data[data$age >= 18, ]
write_spss(data_clean, "survey_adults.sav")

# Export with zlib compression (smaller file, requires SPSS v21+)
write_spss(data, "survey_compressed.zsav", compress = "zsav")
}

}
\seealso{
\code{\link[=read_spss]{read_spss()}} for importing SPSS files,
\code{\link[=write_xlsx]{write_xlsx()}} for Excel export,
\code{\link[=write_stata]{write_stata()}} for Stata export,
\code{\link[=write_xpt]{write_xpt()}} for SAS transport export,
\code{\link[=untag_na]{untag_na()}}, \code{\link[=strip_tags]{strip_tags()}}

Other data-export: 
\code{\link{write_stata}()},
\code{\link{write_xlsx}()},
\code{\link{write_xpt}()}
}
\concept{data-export}
