% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chisq_gof.R
\name{chisq_gof}
\alias{chisq_gof}
\title{Chi-Square Goodness-of-Fit Test}
\usage{
chisq_gof(data, ..., expected = NULL, weights = NULL)
}
\arguments{
\item{data}{Your survey data (data frame or tibble)}

\item{...}{One or more categorical variables to test (tidyselect supported)}

\item{expected}{Optional numeric vector of expected proportions (must sum to 1).
Only used when a single variable is tested. If NULL (default), equal
proportions are assumed.}

\item{weights}{Optional survey weights for population-representative results}
}
\value{
Test results showing whether observed frequencies match expected,
including:
\itemize{
\item Chi-square statistic and p-value for each variable
\item Degrees of freedom
\item Frequency table with observed, expected, and residual counts
\item Sample size (N)
}
}
\description{
\code{chisq_gof()} tests whether observed frequencies of a categorical
variable match expected frequencies. By default, it tests against equal
proportions (uniform distribution). You can also specify custom expected
proportions.

Think of it as:
\itemize{
\item Testing whether categories are equally distributed
\item Comparing observed distribution to a theoretical distribution
\item The one-sample version of the chi-square test
}

The test tells you:
\itemize{
\item Whether observed frequencies differ from expected frequencies
\item How strong the deviation is (chi-square statistic)
\item A frequency table with observed, expected, and residual counts
}
}
\details{
\subsection{Understanding the Results}{

\strong{P-value}: If p < 0.05, the distribution differs from expected
\itemize{
\item p < 0.001: Very strong evidence the distribution differs
\item p < 0.01: Strong evidence the distribution differs
\item p < 0.05: Moderate evidence the distribution differs
\item p >= 0.05: No significant deviation from expected distribution
}

\strong{Residuals}: The difference between observed and expected counts.
Large positive residuals indicate a category has more cases than expected;
large negative residuals indicate fewer cases than expected.
}

\subsection{When to Use This}{

Use this test when:
\itemize{
\item You want to check whether a categorical variable follows a specific distribution
\item You want to test if categories are equally distributed (uniform)
\item You have a single categorical variable and a hypothesised distribution
}
}

\subsection{The Chi-Square Goodness-of-Fit Statistic}{

\deqn{\chi^2 = \sum \frac{(O_i - E_i)^2}{E_i}}

where O_i = observed frequency, E_i = expected frequency.

Degrees of freedom = number of categories - 1.
}

\subsection{Relationship to Other Tests}{
\itemize{
\item For testing association between two categorical variables:
Use \code{\link{chi_square}()} instead
\item For testing a single binary proportion:
Use \code{\link{binomial_test}()} instead
\item For small samples where expected frequencies are below 5:
Use \code{\link{fisher_test}()} instead
}
}

\subsection{SPSS Equivalent}{

SPSS: \code{NPAR TESTS /CHISQUARE=variable /EXPECTED=EQUAL}
or: \code{NPAR TESTS /CHISQUARE=variable /EXPECTED=50 30 20}
}
}
\examples{
# Load required packages and data
library(dplyr)
data(survey_data)

# Test whether gender is equally distributed
survey_data \%>\%
  chisq_gof(gender)

# Test multiple variables at once
survey_data \%>\%
  chisq_gof(gender, region, education)

# Custom expected proportions
survey_data \%>\%
  chisq_gof(interview_mode, expected = c(0.5, 0.3, 0.2))

# With weights
survey_data \%>\%
  chisq_gof(gender, weights = sampling_weight)

# Grouped analysis
survey_data \%>\%
  group_by(region) \%>\%
  chisq_gof(education)

}
\references{
Pearson, K. (1900). On the criterion that a given system of deviations from
the probable in the case of a correlated system of variables is such that it
can be reasonably supposed to have arisen from random sampling.
\emph{Philosophical Magazine}, 50(302), 157-175.
}
\seealso{
\code{\link{chi_square}} for chi-square test of independence (two variables).

\code{\link{binomial_test}} for testing a single proportion.

Other hypothesis_tests: 
\code{\link{binomial_test}()},
\code{\link{chi_square}()},
\code{\link{fisher_test}()},
\code{\link{friedman_test}()},
\code{\link{kruskal_wallis}()},
\code{\link{mann_whitney}()},
\code{\link{mcnemar_test}()},
\code{\link{oneway_anova}()},
\code{\link{t_test}()},
\code{\link{wilcoxon_test}()}
}
\concept{hypothesis_tests}
