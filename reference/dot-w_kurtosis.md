# Weighted kurtosis (SPSS Type-2; delegates to .calc_kurtosis)

Weighted kurtosis (SPSS Type-2; delegates to .calc_kurtosis)

## Usage

``` r
.w_kurtosis(x, weights = NULL, na.rm = TRUE, excess = TRUE)
```
