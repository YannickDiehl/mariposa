# Print compact one-liner for a single EFA result

Print compact one-liner for a single EFA result

## Usage

``` r
.print_efa_compact(
  res,
  n_vars,
  extraction_label,
  rotation_label,
  weighted_tag,
  digits
)
```
