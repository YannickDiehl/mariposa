# Evaluate weights in summarise() context

Evaluate weights in summarise() context

## Usage

``` r
.evaluate_weights(weights_arg, parent_env = parent.frame())
```

## Arguments

- weights_arg:

  Quoted expression for weights

- parent_env:

  Parent environment to search for weights

## Value

Numeric vector of weights or NULL
