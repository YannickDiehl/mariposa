# Print results table with consistent formatting

Print results table with consistent formatting

## Usage

``` r
print_results_table(df, digits = 3, row.names = FALSE)
```

## Arguments

- df:

  Data frame to print

- digits:

  Number of decimal places for numeric columns

- row.names:

  Whether to show row names
