# Compute EFA for a single group

Compute EFA for a single group

## Usage

``` r
.efa_core(
  data,
  var_names,
  weights_vec,
  n_factors,
  rotation,
  extraction,
  use,
  sort,
  blank,
  na.rm
)
```
