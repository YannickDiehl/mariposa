# Truncate labels that exceed a maximum width

Truncate labels that exceed a maximum width

## Usage

``` r
.truncate_labels(labels, max_width = 20)
```

## Arguments

- labels:

  Character vector of labels

- max_width:

  Maximum character width

## Value

Character vector with truncated labels
