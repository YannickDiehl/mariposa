# Compute Bartlett's Test of Sphericity

Tests whether the correlation matrix is significantly different from an
identity matrix. SPSS-compatible formula.

## Usage

``` r
.compute_bartlett(cor_mat, n, k)
```
