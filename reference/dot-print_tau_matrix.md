# Internal function to print correlation matrices without cutoff

Internal function to print correlation matrices without cutoff

## Usage

``` r
.print_tau_matrix(
  mat,
  digits = 3,
  title = "Kendall's Tau Matrix:",
  type = "correlation"
)
```
