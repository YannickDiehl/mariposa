# Print verbose spearman_rho output for one group

Print verbose spearman_rho output for one group

## Usage

``` r
.print_spearman_verbose(x, corrs, matrix_idx, show_cor, show_p, show_n, digits)
```
