# Core pairwise regression computation (matching SPSS /MISSING PAIRWISE)

Core pairwise regression computation (matching SPSS /MISSING PAIRWISE)

## Usage

``` r
.lm_core_pairwise(
  data,
  dep_name,
  pred_names,
  weights_vec,
  standardized,
  conf.level
)
```
