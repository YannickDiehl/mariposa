# Print grouped data header

Print grouped data header

## Usage

``` r
print_group_header(group_values, prefix = "Group")
```

## Arguments

- group_values:

  Named vector or data frame of group values

- prefix:

  Text to print before group info
