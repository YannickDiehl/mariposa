# Print a loading matrix with blank suppression and optional sorting

Print a loading matrix with blank suppression and optional sorting

## Usage

``` r
.print_loading_matrix(mat, blank = 0.4, sort_loadings = TRUE, digits = 3)
```
