# ML extraction for EFA

Maximum Likelihood extraction using stats::factanal() (SPSS /EXTRACTION
ML). Provides goodness-of-fit testing.

## Usage

``` r
.efa_extract_ml(cor_mat, k, n_factors, n_obs, var_names)
```
