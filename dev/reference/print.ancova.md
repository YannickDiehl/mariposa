# Print ANCOVA results

Print ANCOVA results

## Usage

``` r
# S3 method for class 'ancova'
print(x, digits = 3, ...)
```

## Arguments

- x:

  An object of class `"ancova"`.

- digits:

  Number of decimal places (default: 3).

- ...:

  Additional arguments (currently unused).

## Value

Invisibly returns the input object `x`.
