# Compute estimated marginal means (adjusted for covariates at their grand mean)

Compute estimated marginal means (adjusted for covariates at their grand
mean)

## Usage

``` r
.compute_estimated_marginal_means(
  model,
  data,
  dv_name,
  between_names,
  covariate_names,
  w_name
)
```
