# Build ANCOVA table from lm model and drop1 results

Build ANCOVA table from lm model and drop1 results

## Usage

``` r
.build_ancova_table(
  model,
  type3,
  between_names,
  covariate_names,
  dv_name,
  n_total,
  w_name
)
```
