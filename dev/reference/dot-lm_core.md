# Core linear regression computation

Core linear regression computation

## Usage

``` r
.lm_core(
  data,
  formula,
  dep_name,
  pred_names,
  weights_vec,
  use,
  standardized,
  conf.level
)
```
