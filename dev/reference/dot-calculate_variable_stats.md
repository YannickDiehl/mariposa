# Calculate all requested statistics for a single variable

Calculate all requested statistics for a single variable

## Usage

``` r
.calculate_variable_stats(x, w, var_name, show, probs, na.rm, excess)
```
