# Compute coefficients table with standardized coefficients

Compute coefficients table with standardized coefficients

## Usage

``` r
.lm_coefficients(
  model,
  model_summary,
  data,
  dep_name,
  pred_names,
  weights_vec,
  standardized,
  conf.level
)
```
